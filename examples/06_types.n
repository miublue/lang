extern
    fun putnum(n: int),
    fun putstr(s: *char);

fun add(a: int, b: int): int {
    return a + b;
}

fun main() {
    a: int = add(33, 36);
    b: *char = "Hello, World!\n";
    putnum(a);
    putstr(b);
    # putnum(b); # this should fail
}
