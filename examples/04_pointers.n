extern fun putnum(n: int);

fun main() {
    first: int = 420;
    a: int = 36;
    # XXX: 'ptr' type should keep track of which type it points to
    b: *int = &a;
    *b = 69;
    putnum(*(b + 8));
    return 0;
}

