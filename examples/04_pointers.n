extern putnum;
# extern fun putnum(n: int)

fun main() {
    a = 36;
    # XXX: add semicolons as statement separators
    # XXX: 'ptr' type should keep track of which type it points to
    b: ptr = &a;
    @b = 69;
    putnum(@b);
    return 0;
}

