extern fun putnum(n: int);

fun fib(n: int) {
    a: int = 0;
    b: int = 1;
    while (a < n) {
        putnum(a);
        c: int = a + b;
        a = b;
        b = c;
    }
}

fun main() fib(255);

