extern fun putnum(n: int);

fun fib(n: int) {
    # XXX: some way to define multiple variables of same type in one line
    a: int = 0;
    b: int = 1;
    c: int;
    while (a < n) {
        putnum(a);
        c = a + b;
        a = b;
        b = c;
    }
}

fun main() fib(255);

